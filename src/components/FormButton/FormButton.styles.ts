import { styled } from '@mui/material/styles';

import UiButton from '@components/UiButton/UiButton.component';

export const StyledFormButton = styled(UiButton)(() => ({
    minHeight: '5.2rem',
    fontSize: '1.6rem',
}));
