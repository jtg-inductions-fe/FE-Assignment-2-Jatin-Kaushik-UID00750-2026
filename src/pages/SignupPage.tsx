import { SignupForm } from '@containers/SignupForm';

/**
 * Page component for new user registration.
 * Renders the primary registration form container.
 */
export const SignupPage = () => (
    <div>
        <SignupForm />
    </div>
);
