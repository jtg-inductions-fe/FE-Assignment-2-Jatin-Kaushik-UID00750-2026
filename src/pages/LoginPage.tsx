import { LoginForm } from '@containers/LoginForm';

/**
 * Page component for user authentication.
 * Renders the primary login form container.
 */
export const LoginPage = () => (
    <div>
        <LoginForm />
    </div>
);
